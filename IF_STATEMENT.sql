SELECT * FROM MINIONS;

SELECT 
    NAME, IF(AGE = 10, 'ADULT', 'CHILDREN')
FROM
    MINIONS;
    
SELECT
	NAME, IF(STRCMP('BORKO', NAME) = 0, 'TRUE', 'FALSE')
FROM MINIONS;

-- RAND
SELECT FLOOR(RAND() * 10);

-- MAX
SELECT MAX(AGE) AS MAX, MIN(AGE) AS MIN FROM MINIONS;

-- NOW DATE
SELECT CAST(NOW() AS DATE);

SELECT VERSION();














